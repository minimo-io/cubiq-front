// import { postgreService } from '$lib/databases/postgre.service';

export class ClientsCareService {
	// async listForCompany(companyId: string) {
	// 	return await postgreService.execute(async (knex) => {
	// 		const clients = await knex('CareSync_Device_Contacts as c')
	// 			.select('c.id', 'c.name')
	// 			.where('company_id', companyId)
	// 			.orderBy('c.name', 'asc');
	// 		return clients.map((c) => ({ id: c.id, name: c.name }));
	// 	});
	// }
}
